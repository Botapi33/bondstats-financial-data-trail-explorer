name: Financial Data Trail Health

on:
  workflow_dispatch:
  schedule:
    - cron: "17 */6 * * *"

permissions:
  contents: write

concurrency:
  group: financial-data-trail-health
  cancel-in-progress: false

jobs:
  update-lineage-health:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Update source health metadata
        run: node scripts/update-lineage-health.mjs

      - name: Commit changed health snapshot
        run: |
          if git diff --quiet -- public/data/lineage-health.json; then
            echo "No health snapshot changes."
            exit 0
          fi

          git config user.name "bondstats-data-bot"
          git config user.email "actions@users.noreply.github.com"

          git add public/data/lineage-health.json
          git commit -m "chore: update financial data trail health"
          git push
